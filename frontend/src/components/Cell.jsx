//Cada cédula do Sudoku
import React from "react";

function Cell({ row, col }) {
    return(
        <div className="cell">
            //escrever código
        </div>
    );
}

export default Cell;